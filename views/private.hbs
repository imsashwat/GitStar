<!DOCTYPE html>
<html lang="en">

<head>
  <title>GitStar</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="./jquery-3.2.1.js"></script>
  <link rel="stylesheet" href="../2style.css">
</head>

<body bgcolor=#ffffff>
  <div>
    <a href="/"><img src="#" /></a>

    <div class="header">
      <div>
        <img src="{{ metadata.avatar }}" height="200" />
        <div>{{ user.name }}</div>
      </div>
      <div>is</div>
      <div>
        <img src="{{  metadata.artist.id  }}.jpg " height="200" />
        <div class="artist-name">{{ metadata.artist.character }}</div>
      </div>

      <div>{{ metadata.artist.desc }}</div>



      <div>TOTAL STARS</div>

      <div class="meta-value numscroller" data-max="{{ metadata.stargazers_count }}" data-min="0" data-delay="1"
        data-increment="{{ metadata.increment}}">
        {{ metadata.stargazers_count }}
      </div>


      <div class="meta-info inline-block">

        <div class="meta-title">TOTAL FOLLOWERS</div>

        <div class="meta-value numscroller" data-max="{{ user.followers }}" data-min="0" data-delay="1"
          data-increment="{{ metadata.follow }}">
          {{ user.followers }}
        </div>

      </div>
      <div class="meta-info inline-block">

        <div class="meta-title">TOTAL REPOS (PUBLIC)</div>

        <div data-max="{{ user.public_repos }}" data-increment="{{ metadata.repos  }}">
          {{ user.public_repos }}

        </div>
      </div>
      <div>
        <div>FAVORITE LANGUAGE</div>
        <div>{{ metadata.mainlanguage }}</div>
      </div>
    </div>
</body>

</html>